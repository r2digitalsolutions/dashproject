export const createDatabase = async (database: any) => {
  // Aquí iría la lógica para crear una base de datos en la base de datos
  console.log('Creando base de datos:', database);
  return database;
};